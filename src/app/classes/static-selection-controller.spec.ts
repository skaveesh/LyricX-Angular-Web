import { StaticSelectionController } from './static-selection-controller';

describe('StaticSelectionController', () => {
  it('should create an instance', () => {
    expect(new StaticSelectionController(null, null, null)).toBeTruthy();
  });
});
