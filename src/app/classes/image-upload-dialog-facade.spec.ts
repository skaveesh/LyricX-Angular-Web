import { ImageUploadDialogFacade } from './image-upload-dialog-facade';

describe('ImageUploadDialogFacade', () => {
  it('should create an instance', () => {
    expect(new ImageUploadDialogFacade()).toBeTruthy();
  });
});
