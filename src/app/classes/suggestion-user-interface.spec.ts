import { SuggestionUserInterface } from './suggestion-user-interface';

describe('SuggestionUserInterface', () => {
  it('should create an instance', () => {
    expect(new SuggestionUserInterface(null)).toBeTruthy();
  });
});
