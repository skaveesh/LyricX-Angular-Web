import { StaticSelectionAdapter } from './static-selection-adapter';

describe('StaticSelectionAdapter', () => {
  it('should create an instance', () => {
    // expect(new StaticSelectionAdapter()).toBeTruthy();
  });
});
